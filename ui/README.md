# Web UI

The web UI is built from the Cloud-Dog UI monorepo. The compiled bundle (`ui/dist/`) is a build artefact generated during the container build and is not committed to the public source mirror.
